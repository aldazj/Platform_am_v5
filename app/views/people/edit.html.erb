<div id="page-wrapper">
  <div class="row">
    <div class="col-lg-12">
      <h1>Edit Users  <small>Editing Users</small></h1>
      <ol class="breadcrumb">
        <li class="active"><i class="fa fa-dashboard"></i> Editing Users </li>
      </ol>
    </div>
  </div>

  <%= render 'form' %>

  <%= link_to 'Show', @person, class:'btn btn-default' %>
  <% if current_person.type == 'Admin' %>
    <%= link_to 'Back', people_path, class:'btn btn-default' %>
  <% end %>
</div>